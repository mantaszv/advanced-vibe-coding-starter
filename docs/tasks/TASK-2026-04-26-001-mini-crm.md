---
id: TASK-2026-04-26-001
title: mini-CRM Lean MVP — implementation task list
status: ready
phase: 2 (tasks)
date: 2026-04-26
revised: 2026-04-26 (Karpathy Simplicity First peržiūra: drop'inta ~30% scope'o)
related_prd: docs/requirements/REQ-2026-04-26-001-mini-crm.md
related_spec: docs/superpowers/specs/2026-04-26-mini-crm-design.md
total_parent_tasks: 14 aktyvūs + 1 atidėtas (TASK-12 → V1.5)
estimated_total_loc: ~4500–5500 (src + tests)
---

# Tasks for REQ-2026-04-26-001 — mini-CRM Lean MVP

> Phase 2 task list. Kiekvienas parent task = atskira PR (arba grupė PR'ų).
> Sub-tasks granuliarūs: vienas sub-task = vienas konkretus pakeitimas konkrečiame faile.

**Risk legend:** 🔴 HIGH · 🟡 MED · 🟢 LOW

## Relevant files

| Failas | Paskirtis |
|---|---|
| `package.json`, `tsconfig.json` | Tooling config |
| `app/globals.css` | Tailwind v4 `@theme` (CSS-first) |
| `app/layout.tsx`, `app/page.tsx` | Root layout + landing |
| `app/(auth)/prisijungti/page.tsx` | Login |
| `app/onboarding/workspace/page.tsx` | Workspace creation |
| `app/app/[slug]/**` | Authenticated app routes |
| `actions/*.ts` | Server Actions per domain |
| `lib/supabase/{server,browser,middleware}.ts` | Supabase clients |
| `lib/validation/*.ts` | zod schemas |
| `lib/auth/{require-session,require-org,require-active-subscription}.ts` | Auth guards |
| `supabase/migrations/*.sql` | DB schema + RLS |
| `app/api/stripe/webhook/route.ts` | Stripe webhook (Vercel Function, runtime=nodejs) |
| `tests/rls/*.sql` | pgTAP RLS testai |
| `tests/e2e/*.spec.ts` | Playwright critical scenarios |
| `.github/workflows/{ci,deploy}.yml` | CI/CD |

### Notes

- Test coverage ≥ 80% paveiktiems failams (CLAUDE.md privaloma)
- File-size-guard: ≤ 300 LOC/file, ≤ 200 LOC/component, ≤ 50 LOC/function
- Visi UI tekstai LT su diakritika; URL slug'ai LT be diakritikos
- Migracijos privalo turėti UP+DOWN per `db-migration-guard`

---

## Tasks

### 🟢 TASK-01: Project scaffold + tooling
**Priklausomybės:** nėra · **LOC:** ~250 · **Risk:** LOW

- **Orchestration:**
  - _MCP_: Context7 (`query-docs` — Next.js 16.2 init, Tailwind v4 Vite plugin); Vercel (`link`)
  - _Agents_: `dependency-guardian`, `general-purpose`
  - _Guards_: `dependency-guardian` (PRE), `pre-deploy` (POST)
  - _Skills_: `tailwind-v4-shadcn`, `vercel:nextjs`
  - _Verify_: `npm run build`, `npm run lint`, `npm run dev` (port 3000)

- [x] **01.1** `npx create-next-app@latest . --typescript --tailwind --app --no-src-dir --turbopack` → init projektą
- [x] **01.2** Update `package.json`: pin `next@16.2.x`, `react@19.2.x`, `tailwindcss@^4`, `@tailwindcss/postcss@^4`
- [x] **01.3** Sukurti `app/globals.css` su `@import "tailwindcss";` + `@theme inline { --color-brand-* (OKLCH); }` (≤ 80 LOC) [BLOCKS: 01.4]
- [x] **01.4** `npx shadcn@latest init --base-color slate --style default` → config
- [x] **01.5** Add shadcn components: `button card dialog input label select sonner badge separator dropdown-menu`
- [x] **01.6** Add deps: `zod @dnd-kit/core @dnd-kit/sortable @tanstack/react-table` + types
- [x] **01.7** Add dev deps: `vitest @vitest/ui @testing-library/react @playwright/test`
- [x] **01.8** Sukurti `vitest.config.ts` su jsdom env + path aliases (`@/` → root)
- [x] **01.9** Sukurti `playwright.config.ts` su chromium + webkit projects, `webServer` config
- [x] **01.10** ESLint config: `next/core-web-vitals` + `@typescript-eslint/strict`; Prettier config
- [x] **01.11** Sukurti `.env.example` su visais env vars (žr. 02.4) + komentarai
- [x] **01.12** `npm scripts`: `dev`, `build`, `lint`, `test`, `test:e2e`, `test:rls`, `db:reset`, `db:types`
- [x] **01.13** Renderint shadcn Button `app/page.tsx` (smoke test) + `app/layout.tsx` su LT lang attr
- [x] **01.14** Tests: `tests/smoke.test.ts` — vitest renderina Button, asserts LT text

**Risk per sub-task:**

| Sub | Risk | Issue | Mitigation |
|---|---|---|---|
| 01.3 | 🟡 | Tailwind v4 OKLCH spalvos Safari < 16.4 | `@supports` fallback su sRGB |
| 01.4 | 🟢 | shadcn CLI v0.10+ formatai keičiasi | `tailwind-v4-shadcn` skill |

---

### 🟢 TASK-02: Supabase setup + env config
**Priklausomybės:** TASK-01 · **LOC:** ~200 · **Risk:** LOW

- **Orchestration:**
  - _MCP_: Supabase (`list_projects`, `get_project_url`, `get_publishable_keys`, `generate_typescript_types`)
  - _Agents_: `general-purpose`
  - _Guards_: `security-guard` (PRE — secrets in `.gitignore`)
  - _Skills_: `supabase-backend-platform`, `vercel:env-vars`
  - _Verify_: `npx supabase start`, `npx supabase status`, `node scripts/verify-env.mjs`

- [ ] **02.1** `npx supabase init` → `supabase/config.toml`
- [ ] **02.2** `supabase/config.toml`: enable Auth Hooks block (`[auth.hook.custom_access_token]`)
- [ ] **02.3** `npx supabase start` — local DB on :54322
- [ ] **02.4** Env vars (`.env.local` + `.env.example`): `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `STRIPE_PRICE_ID_MONTHLY`, `STRIPE_PRICE_ID_YEARLY`, `RESEND_API_KEY`, `NEXT_PUBLIC_SITE_URL`
- [ ] **02.5** `lib/supabase/server.ts` (≤ 60 LOC) — `createServerClient` su `cookies()` from `next/headers`
- [ ] **02.6** `lib/supabase/browser.ts` (≤ 30 LOC) — `createBrowserClient` (kanban only)
- [ ] **02.7** `lib/supabase/middleware.ts` (≤ 80 LOC) — `updateSession` helper for `middleware.ts`
- [ ] **02.8** `middleware.ts` (root, ≤ 40 LOC) — invokes `updateSession`, defines matcher
- [ ] **02.9** `scripts/verify-env.mjs` — checks all env vars set, exits 1 if missing
- [ ] **02.10** `package.json:scripts.db:types` → `supabase gen types typescript --local > packages/db-types/database.ts`
- [ ] **02.11** Initial run `npm run db:types` — generates empty types (no tables yet)

**Risk per sub-task:**

| Sub | Risk | Issue | Mitigation |
|---|---|---|---|
| 02.4 | 🟢 | Service role key leak | `.env.local` in `.gitignore`; verify pre-commit |
| 02.7 | 🟡 | Middleware infinite redirect (FR-2) | Eksplicit `next` param; max-age cookie |

---

### 🔴 TASK-03: DB schema — tenancy core + RLS + Auth Hook
**Priklausomybės:** TASK-02 · **LOC:** ~350 SQL · **Risk:** HIGH

- **Orchestration:**
  - _MCP_: Supabase (`apply_migration`, `get_advisors`, `execute_sql`)
  - _Agents_: `db-migration-guard`, `security-engineer`, `backend-architect`
  - _Guards_: `db-guardian` (PRE+POST), `security-guard` (PRE), `risk-assessor` (PRE)
  - _Skills_: `supabase-backend-platform`
  - _Verify_: `supabase db reset`, `psql ... \d+ orgs` (force_rls=true), `pgTAP cross-tenant test`

- [ ] **03.1** Migracija `supabase/migrations/<ts>_001_tenancy_up.sql`: `orgs` lentelė (≤ 30 LOC) [BLOCKS: 03.2]
- [ ] **03.2** Add `org_members` lentelė + composite PK + indeksai (≤ 25 LOC) [BLOCKS: 03.3]
- [ ] **03.3** Add `org_invites` lentelė + indeksai (≤ 25 LOC)
- [ ] **03.4** `auth.org_id()` helper function su `current_setting('request.jwt.claims', true)::jsonb` (≤ 15 LOC)
- [ ] **03.5** `public.custom_access_token_hook(event jsonb)` function — užkrauna pirmąjį joined `org_id` į `app_metadata` (≤ 25 LOC) [BLOCKS: 03.6]
- [ ] **03.6** `grant execute on function public.custom_access_token_hook to supabase_auth_admin` + register hook URL
- [ ] **03.7** RLS policies `orgs`: 4 policies (SELECT/INSERT/UPDATE/DELETE) tik kur `id = (select auth.org_id())`; `force RLS` (≤ 30 LOC)
- [ ] **03.8** RLS policies `org_members`: SELECT visiems org members; INSERT/UPDATE/DELETE tik owner/admin (≤ 40 LOC)
- [ ] **03.9** RLS policies `org_invites`: tik owner/admin sees + manages (≤ 30 LOC)
- [ ] **03.10** `<ts>_001_tenancy_down.sql` rollback su DROP order: hooks → policies → functions → tables (≤ 25 LOC)
- [ ] **03.11** Apply migration: `supabase db reset` → asserts no errors
- [ ] **03.12** `npm run db:types` regeneruoja `packages/db-types/database.ts`
- [ ] **03.13** Smoke pgTAP `tests/rls/_smoke.sql`: SELECT works, RLS active

**Risk per sub-task:**

| Sub | Risk | Issue | Mitigation |
|---|---|---|---|
| 03.5 | 🔴 | Auth Hook nepasileidžia → JWT be `org_id` → empty CRM | Integration test (TASK-06.10) |
| 03.7 | 🔴 | RLS policy gap → cross-tenant leak | pgTAP TASK-04, CI gate |
| 03.10 | 🟡 | DROP order klaida → migration stuck | Test `supabase db reset` 2x |

---

### 🟡 TASK-04: pgTAP RLS testing infrastructure
**Priklausomybės:** TASK-03 · **LOC:** ~300 SQL+JS · **Risk:** MED

- **Orchestration:**
  - _MCP_: Supabase (`execute_sql`)
  - _Agents_: `quality-engineer`, `test-quality-guardian`
  - _Guards_: `test-quality-guardian` (POST), `pre-deploy` (POST)
  - _Skills_: `superpowers:test-driven-development`
  - _Verify_: `npm run test:rls`, `scripts/check-rls-completeness.sql`

- [ ] **04.1** `tests/rls/_helpers.sql` (≤ 80 LOC) — `tests.create_supabase_user(email, org_slug)`, `tests.authenticate_as(email)`, `tests.set_jwt_claim(org_id)`
- [ ] **04.2** `tests/rls/tenancy_test.sql` (≤ 100 LOC) — 4 scenarijai per Section 9.2 PRD: Alice mato, Bob nemato, INSERT into Alice org throws, UPDATE/DELETE blocked
- [ ] **04.3** `package.json:scripts.test:rls` → `psql -f tests/rls/run-all.sql`
- [ ] **04.4** `tests/rls/run-all.sql` — orchestruoja `_helpers` + visus `*_test.sql`
- [ ] **04.5** GitHub Actions workflow stub `.github/workflows/rls-tests.yml` — boots Supabase local + runs test:rls

> **Drop'inta (Simplicity):** `scripts/check-rls-completeness.sql` meta-checker — pakeičia PR review checklist'as `.github/PULL_REQUEST_TEMPLATE.md` faile (0 LOC).

**Risk per sub-task:**

| Sub | Risk | Issue | Mitigation |
|---|---|---|---|
| 04.4 | 🟢 | pgTAP rollback klaida tarp testų | `begin ... rollback` per test |

---

### 🔴 TASK-05: Auth flow + middleware
**Priklausomybės:** TASK-04 · **LOC:** ~400 · **Risk:** HIGH

- **Orchestration:**
  - _MCP_: Supabase (Auth Hook config); Context7 (`query-docs` — Supabase Auth Hooks 2026)
  - _Agents_: `security-engineer`, `frontend-architect`
  - _Guards_: `security-guard` (PRE+POST), `risk-assessor` (PRE)
  - _Skills_: `supabase-backend-platform`, `vercel:auth`
  - _Verify_: Playwright signup flow, JWT decode shows `app_metadata.org_id` (after TASK-06)

- [ ] **05.1** `app/(auth)/layout.tsx` (≤ 30 LOC) — minimal layout
- [ ] **05.2** `app/(auth)/prisijungti/page.tsx` (≤ 120 LOC) — email form + Google OAuth button
- [ ] **05.3** `actions/auth.ts:signInWithEmail(email)` (≤ 40 LOC) — `supabase.auth.signInWithOtp({ emailRedirectTo })`
- [ ] **05.4** `actions/auth.ts:signInWithGoogle()` (≤ 30 LOC) — `signInWithOAuth({ provider: 'google' })`
- [ ] **05.5** `app/auth/callback/route.ts` (≤ 50 LOC) — exchange code for session, redirect to `/onboarding/workspace` or `/app/{slug}`
- [ ] **05.6** Resend email template (LT) — `supabase/templates/magic-link.html` su diakritika
- [ ] **05.7** `lib/auth/require-session.ts` (≤ 30 LOC) — throws or redirects `/prisijungti`
- [ ] **05.8** `lib/auth/require-org.ts` (≤ 40 LOC) — throws if user not org_member of slug param
- [ ] **05.9** Update `middleware.ts` — protect `/app/**`, `/onboarding/**`; allow `/`, `/(auth)/**`, `/api/stripe/**`
- [ ] **05.10** `app/(auth)/kvietimas/[token]/page.tsx` (≤ 80 LOC) — accept invite (validates token, calls `acceptInvite` action)
- [ ] **05.11** `actions/invites.ts:acceptInvite(token)` (≤ 80 LOC) — validate, INSERT org_members, mark invite accepted, refreshSession, redirect
- [ ] **05.12** Tests `tests/auth.test.ts` — magic link flow, OAuth callback parsing

**Risk per sub-task:**

| Sub | Risk | Issue | Mitigation |
|---|---|---|---|
| 05.5 | 🔴 | Callback klaida palieka user be sesijos | E2E test su error states |
| 05.6 | 🟡 | LT diakritika spam'ą trigger'ina | Resend monitoring (PRD Q4) |
| 05.9 | 🟡 | Matcher klaida → infinite redirect | Eksplicit allowlist |
| 05.11 | 🔴 | Token replay attack | `expires_at` + `accepted_at` check |

---

### 🔴 TASK-06: Onboarding + workspace creation
**Priklausomybės:** TASK-05 · **LOC:** ~350 · **Risk:** HIGH (FR-4)

- **Orchestration:**
  - _MCP_: Stripe (`customers.create`)
  - _Agents_: `backend-architect` (atomic tx), `security-engineer`
  - _Guards_: `security-guard` (PRE), `risk-assessor` (PRE)
  - _Skills_: `superpowers:test-driven-development`
  - _Verify_: Playwright onboarding → empty CRM not seen; Vitest concurrent slug

- [ ] **06.1** `app/onboarding/layout.tsx` — auth-only, redirects if already in org
- [ ] **06.2** `app/onboarding/workspace/page.tsx` (≤ 120 LOC) — form (name, slug, currency, team_size)
- [ ] **06.3** `lib/validation/workspace.ts` — zod: name 2-50, slug regex `[a-z0-9-]+` 3-30, currency enum, team_size enum
- [ ] **06.4** `actions/workspace.ts:slugifyName(name)` (≤ 15 LOC) — slugify only; unikalumą validuoja Postgres unique constraint, error grąžinamas formai ("Šis slug užimtas, pasirink kitą")
- [ ] **06.5** `actions/workspace.ts:createWorkspace(input)` (≤ 100 LOC) — atomic tx via Postgres function `create_workspace_tx(name, slug, currency, owner_id)`; on `unique_violation` → form error per RHF [BLOCKS: 06.6]
- [ ] **06.6** Migration `<ts>_002_create_workspace_fn_up.sql` — function INSERT orgs + org_members(owner) + 5 default pipeline_stages atomically (≤ 60 LOC)
- [ ] **06.7** Stripe customer creation in `createWorkspace` po DB tx success (NE atomically — Stripe failure tolerable)
- [ ] **06.8** UPDATE `orgs.stripe_customer_id` after Stripe response
- [ ] **06.9** `await supabase.auth.refreshSession()` + `revalidatePath('/app', 'layout')` + `redirect('/app/{slug}')`
- [ ] **06.10** Integration test `tests/integration/onboarding.test.ts` (≤ 100 LOC) — signup → createWorkspace → JWT decode shows `app_metadata.org_id` → list contacts (RLS pass)
- [ ] **06.11** Playwright `tests/e2e/onboarding.spec.ts` — full flow signup → workspace → kanban (empty state)

**Risk per sub-task:**

| Sub | Risk | Issue | Mitigation |
|---|---|---|---|
| 06.5 | 🔴 | refreshSession nepasileidžia → empty CRM (FR-4) | Test 06.10 mandatory |
| 06.4 | 🟢 | Slug race: 2 concurrent same name | `unique_violation` surfaces kaip form error — user'is pasirenka kitą |
| 06.7 | 🟡 | Stripe down → workspace partial state | Retry + manual sync from `/atsiskaitymas` |

---

### 🔴 TASK-07: CRM core data layer
**Priklausomybės:** TASK-04 · **LOC:** ~600 SQL · **Risk:** HIGH

- **Orchestration:**
  - _MCP_: Supabase (`apply_migration`, `get_advisors`)
  - _Agents_: `db-migration-guard`, `security-engineer`, `backend-architect`
  - _Guards_: `db-guardian` (PRE+POST), `security-guard` (PRE), `risk-assessor` (PRE)
  - _Skills_: `supabase-backend-platform`
  - _Verify_: pgTAP per lentelę (5×4 = 20 testai); `EXPLAIN ANALYZE` su 10K rows shows index scan

- [ ] **07.1** Migracija `<ts>_003_crm_core_up.sql`: `companies` (≤ 25 LOC) [BLOCKS: 07.2]
- [ ] **07.2** Add `contacts` (≤ 30 LOC) — FK į companies
- [ ] **07.3** Add `pipeline_stages` (≤ 20 LOC) — `is_won`, `is_lost` flags
- [ ] **07.4** Add `deals` (≤ 35 LOC) — FK į stages (`ON DELETE RESTRICT`), contact, company
- [ ] **07.5** Add `activities` (≤ 30 LOC) — polymorphic type, FK į contact + deal (CASCADE)
- [ ] **07.6** Indeksai: kiekviena `(org_id, ...)` filtravimo kombinacija (≤ 50 LOC)
- [ ] **07.7** Unique partial: `contacts(org_id, lower(email)) WHERE email is not null`; `companies(org_id, lower(domain)) WHERE domain is not null`
- [ ] **07.8** Update trigger: `set_updated_at()` per kiekvieną lentelę (su trigger function)
- [ ] **07.9** RLS visoms 5 lentelėms: 4 policies + `force RLS` (≤ 200 LOC visiems)
- [ ] **07.10** `<ts>_003_crm_core_down.sql` rollback (DROP order: triggers → policies → tables CASCADE)
- [ ] **07.11** Apply + regen types
- [ ] **07.12** pgTAP `tests/rls/contacts_test.sql`, `companies_test.sql`, `deals_test.sql`, `activities_test.sql`, `pipeline_stages_test.sql` (≤ 80 LOC each)
- [ ] **07.13** Run `scripts/check-rls-completeness.sql` — pass

**Risk per sub-task:**

| Sub | Risk | Issue | Mitigation |
|---|---|---|---|
| 07.4 | 🟡 | `ON DELETE RESTRICT` fails kai admin ištrina stage su deals | UI confirmation prieš delete (FR-26) |
| 07.9 | 🔴 | Trūkstama policy lentelei = leak | `check-rls-completeness.sql` CI gate |
| 07.10 | 🟡 | CASCADE drop order su trigger functions | Test `db reset` |

---

### 🟡 TASK-08: CRM Server Actions + zod validation
**Priklausomybės:** TASK-07 · **LOC:** ~1200 · **Risk:** MED

- **Orchestration:**
  - _MCP_: Context7 (`query-docs` — Server Actions 16.2 patterns)
  - _Agents_: `backend-architect`, `security-engineer`
  - _Guards_: `file-splitter` (PRE), `test-coverage-guard` (POST)
  - _Skills_: `superpowers:test-driven-development`, `vercel:nextjs`
  - _Verify_: Vitest ≥80% coverage, `revalidatePath` works

- [ ] **08.1** `lib/validation/contact.ts` — zod schema (≤ 40 LOC) — first_name, last_name, email (.email().optional()), phone, job_title, company_id (uuid optional)
- [ ] **08.2** `lib/validation/company.ts` — zod (≤ 30 LOC)
- [ ] **08.3** `lib/validation/deal.ts` — zod (≤ 50 LOC) — value_cents bigint, stage_id required
- [ ] **08.4** `lib/validation/activity.ts` — zod discriminated union per type (note/task/meeting) (≤ 60 LOC)
- [ ] **08.5** `lib/validation/pipeline_stage.ts` — zod (≤ 30 LOC)
- [ ] **08.6** `actions/contacts.ts` (≤ 200 LOC) — `createContact`, `updateContact`, `deleteContact`, `listContacts(filters)` su zod parse
- [ ] **08.7** `actions/companies.ts` (≤ 80 LOC) — `createCompany`, `listCompanies(query)` (Combobox autocomplete); be `update`/`delete` UI'jaus V1, naudoti tik per ContactForm Combobox on-the-fly
- [ ] **08.8** `actions/deals.ts` (≤ 250 LOC) — CRUD + `updateDealStage(id, stageId)` (kanban DnD); auto-set `closed_at` if stage `is_won/is_lost`
- [ ] **08.9** `actions/activities.ts` (≤ 200 LOC) — CRUD + `markTaskDone(id)` + polymorphic validation
- [ ] **08.10** `actions/pipeline_stages.ts` (≤ 150 LOC) — CRUD + `reorderStages(orderArray)` su Postgres `update ... from (values ...) as new(id, position)`
- [ ] **08.11** `revalidatePath` kiekvienoj mutation kalbantis su `app/[slug]/{sandoriai,kontaktai,veiklos}`
- [ ] **08.12** Tests: `actions/*.test.ts` — happy path + edge cases (empty inputs, max length, RLS bypass attempt)

**Risk per sub-task:**

| Sub | Risk | Issue | Mitigation |
|---|---|---|---|
| 08.4 | 🟡 | Discriminated union edge cases (note be body) | zod tests 08.12 |
| 08.8 | 🟡 | `updateDealStage` race condition (2 users same deal) | Optimistic concurrency: check `updated_at` |
| 08.10 | 🟡 | Reorder Stages partial update | Single SQL statement, ne loop |

---

### 🟢 TASK-09: UI layout + navigation
**Priklausomybės:** TASK-06 · **LOC:** ~500 · **Risk:** LOW

- **Orchestration:**
  - _MCP_: Context7 (shadcn/ui Sidebar, Tailwind v4 dark mode)
  - _Agents_: `frontend-architect`
  - _Guards_: `lang-reviewer` (POST), `file-splitter` (PRE)
  - _Skills_: `vercel:shadcn`, `frontend-design:frontend-design`
  - _Verify_: Playwright nav links, Lighthouse mobile >95

- [ ] **09.1** `app/app/[slug]/layout.tsx` (≤ 80 LOC) — auth + org check, sidebar + main
- [ ] **09.2** `components/shared/Sidebar.tsx` (≤ 150 LOC) — 4 nav items LT (Sandoriai, Kontaktai, Veiklos, Nustatymai), active route highlight, **org pavadinimas header'yje kaip statinis tekstas (be switcher chrome)**
- [ ] **09.3** `components/shared/EmptyState.tsx` (≤ 60 LOC) — generic empty state su CTA
- [ ] **09.4** `components/shared/OnboardingChecklist.tsx` (≤ 120 LOC) — 3-step (contact, deal, invite) su persistence per localStorage (V1)
- [ ] **09.5** `app/app/[slug]/page.tsx` — redirect to `/app/{slug}/sandoriai`
- [ ] **09.6** Tests: Playwright `tests/e2e/navigation.spec.ts` — visi 4 nav links work

> **Drop'inta (Simplicity):**
> - `OrgSwitcher.tsx` — V1 = 1 org per user, switcher'is bereikalingas; org pavadinimas rodomas kaip statinis tekstas Sidebar header'yje (fold į 09.2).
> - `ThemeToggle.tsx` — atidėta į V1.1 (premature polish, ne MVP).

**Risk per sub-task:**

| Sub | Risk | Issue | Mitigation |
|---|---|---|---|
| 09.2 | 🟢 | LT diakritika icons | shadcn icons separate, text in `<span>` |

---

### 🟡 TASK-10: UI deals kanban (table view DEFERRED to V1.5)
**Priklausomybės:** TASK-08, TASK-09 · **LOC:** ~600 · **Risk:** MED

- **Orchestration:**
  - _MCP_: Context7 (`@dnd-kit/core` patterns, React 19 useOptimistic)
  - _Agents_: `frontend-architect`, `quality-engineer`
  - _Guards_: `lang-reviewer` (POST), `file-splitter` (PRE)
  - _Skills_: `vercel:react-best-practices`
  - _Verify_: Playwright DnD + keyboard a11y, optimistic revert toast

- [ ] **10.1** `app/app/[slug]/sandoriai/page.tsx` (≤ 80 LOC) — RSC fetch deals + stages, pass to client
- [ ] **10.2** `components/kanban/KanbanBoard.tsx` (≤ 180 LOC) — DnDContext + drag handlers (client)
- [ ] **10.3** `components/kanban/KanbanColumn.tsx` (≤ 120 LOC) — droppable column su deals (client)
- [ ] **10.4** `components/kanban/DealCard.tsx` (≤ 150 LOC) — draggable card su title, value, contact
- [ ] **10.5** `hooks/use-optimistic-deals.ts` (≤ 80 LOC) — `useOptimistic` reducer + Server Action call
- [ ] **10.6** Toast on Server Action error (sonner) — revert optimistic state
- [ ] **10.7** Keyboard a11y: Tab, Space, arrow keys (per `@dnd-kit` `KeyboardSensor`)
- [ ] **10.8** `app/app/[slug]/sandoriai/[id]/page.tsx` (≤ 120 LOC) — detail, RHS panel layout
- [ ] **10.9** `components/deals/DealDetailPanel.tsx` (≤ 180 LOC) — tabs: Activities, Details, Edit
- [ ] **10.10** `components/deals/DealForm.tsx` (≤ 180 LOC) — Server Action form
- [ ] **10.11** Empty state ant kanban kai 0 deals
- [ ] **10.12** Tests Playwright `tests/e2e/deals-kanban.spec.ts` — drag, optimistic revert, keyboard a11y

> **Drop'inta (Simplicity):** Table view `sandoriai/sarasas` + `DealsTable.tsx` — kanban yra V1 diferenciatorius, table = V2.

**Risk per sub-task:**

| Sub | Risk | Issue | Mitigation |
|---|---|---|---|
| 10.5 | 🟡 | Optimistic state desync (FR-10) | Reconcile via Server Action return value |
| 10.7 | 🟡 | a11y broken pre-launch | Playwright + screen reader manual test |
| 10.2-10.4 | 🟡 | LOC overflow | Splittable per dnd-kit pattern |

---

### 🟢 TASK-11: UI contacts + activities
**Priklausomybės:** TASK-08, TASK-09 · **LOC:** ~750 · **Risk:** LOW

- **Orchestration:**
  - _MCP_: Context7 (shadcn Form, Dialog, Combobox)
  - _Agents_: `frontend-architect`
  - _Guards_: `lang-reviewer` (POST)
  - _Skills_: `vercel:shadcn`
  - _Verify_: Playwright create→list, search, filter

- [ ] **11.1** `components/shared/DataTable.tsx` (≤ 200 LOC) — generic tanstack wrapper su filter/search/pagination
- [ ] **11.2** `app/app/[slug]/kontaktai/page.tsx` (≤ 100 LOC) — RSC fetch + DataTable
- [ ] **11.3** `components/contacts/ContactForm.tsx` (≤ 180 LOC) — create/edit dialog; **company per shadcn Combobox** (autocomplete iš `companies` lentelės; "Kurti naują" sukurią company on-the-fly per `createCompany` action)
- [ ] **11.4** `app/app/[slug]/kontaktai/[id]/page.tsx` (≤ 150 LOC) — detail su activities sidebar
- [ ] **11.5** `app/app/[slug]/veiklos/page.tsx` (≤ 130 LOC) — visos org veiklos su filtrais: type (note/task/meeting), assignee (visi/man priskirtos), status; default filtras "man priskirtos" jei type=task
- [ ] **11.6** `components/activities/ActivityForm.tsx` (≤ 180 LOC) — type-aware (note/task/meeting)
- [ ] **11.7** `components/activities/ActivityList.tsx` (≤ 100 LOC) — plain text render su `white-space: pre-wrap` (be markdown)
- [ ] **11.8** Tests Playwright: create contact → appears in list, search filters; activity type filter veikia

> **Drop'inta (Simplicity):**
> - Atskira `imones/` (companies) UI sekcija — company auto-kuriama per ContactForm Combobox. DB lentelė `companies` lieka grupavimui/reporting'ui V1.5.
> - `uzduotys/` atskira route'a — fold'inta į `veiklos/` su `type=task&assignee=me` filtru (vienas page, mažiau dubliavimosi).
> - `react-markdown` rendering — V1 plain text + `pre-wrap` (markdown V1.5).

**Risk per sub-task:**

| Sub | Risk | Issue | Mitigation |
|---|---|---|---|
| 11.7 | 🟢 | XSS per user input body | React eskeipuoja `{body}` automatiškai; `white-space: pre-wrap` ne `dangerouslySetInnerHTML` |
| 11.3 | 🟡 | Combobox on-the-fly create race | `createCompany` returns `id`; ContactForm naudoja grąžintą ID |

---

### ⏸️ TASK-12: CSV import flow — **DEFERRED TO V1.5**
**Status:** atidėta · **Priežastis:** ~700 LOC + papaparse + iconv-lite + 3-step wizard + bucket + RLS — per sunkus svoris Lean MVP'ui. Pirmieji 10–20 klientų suvedinės kontaktus rankiniu būdu arba per ContactForm. Re-evaluate po V1 launch'o pagal user feedback.

**V1.5 scope'as kai grįžtama:**
- 3-step wizard (Upload → Mapping → Preview) per `kontaktai/importuoti`
- papaparse + UTF-8 / Windows-1257 detect
- Batch insert su `on conflict (org_id, lower(email)) do update` (auto-merge)
- Error report download

**V1 fallback (jei labai reikia):** vienas `<textarea>` `kontaktai/page.tsx` su placeholderiu `Vardas, Pavardė, El. paštas` — vienas kontaktas per eilutę, parse client'e, batch insert per esamą `createContact` action loop'e (~100 LOC). NEPRIVALOMA — pridėk tik jei pirmas customer paprašys.

---

### 🟡 TASK-13: Settings (team, pipeline editor, profile, billing portal)
**Priklausomybės:** TASK-08, TASK-09 · **LOC:** ~600 · **Risk:** MED

- **Orchestration:**
  - _MCP_: Stripe (`billing.portal.sessions.create`)
  - _Agents_: `backend-architect`, `frontend-architect`
  - _Guards_: `lang-reviewer` (POST), `payment-guardian` (PRE)
  - _Skills_: `superpowers:test-driven-development`
  - _Verify_: Playwright reorder via arrows, Stripe Portal redirect

- [ ] **13.1** `app/app/[slug]/nustatymai/layout.tsx` (≤ 50 LOC) — sub-nav: Profile, Komanda, Pipeline, Atsiskaitymas
- [ ] **13.2** `app/app/[slug]/nustatymai/page.tsx` (≤ 100 LOC) — profile form (name, currency)
- [ ] **13.3** `app/app/[slug]/nustatymai/komanda/page.tsx` (≤ 150 LOC) — members list + invite form
- [ ] **13.4** `actions/invites.ts:createInvite(email, role)` (≤ 80 LOC) — generate token, INSERT, send Resend email LT
- [ ] **13.5** `actions/invites.ts:revokeInvite(id)`, `removeMember(userId)` (≤ 80 LOC)
- [ ] **13.6** `app/app/[slug]/nustatymai/pipeline/page.tsx` (≤ 100 LOC) — stages CRUD + reorder
- [ ] **13.7** `components/settings/PipelineStageEditor.tsx` (≤ 130 LOC) — **up/down arrow buttons** reorder (per `reorderStages` action), inline edit, delete su confirmation modal (jei deals exist)
- [ ] **13.8** `app/app/[slug]/nustatymai/atsiskaitymas/page.tsx` (≤ 80 LOC) — Stripe Portal button + sub status
- [ ] **13.9** `actions/billing.ts:openPortal()` (≤ 40 LOC) — Stripe Customer Portal session, redirect
- [ ] **13.10** Tests: Playwright pipeline reorder via arrows persists; delete stage with deals → confirmation modal blocks

> **Drop'inta (Simplicity):**
> - `pg_advisory_xact_lock(hashtext(org_id))` invite accept'e — `unique constraint (org_id, user_id)` ant `org_members` jau apsaugo nuo dublikato; quantity sync — eventual via Stripe webhook. Pridėti lock'ą tik jei prod metu stebėsi race condition.
> - DnD pipeline reorder per `@dnd-kit` — pakeista į up/down arrow buttons (50 LOC vietoj 200). Admin'as pipeline'us reorder'ina 1× per gyvenimą, DnD UX nereikalingas.

**Risk per sub-task:**

| Sub | Risk | Issue | Mitigation |
|---|---|---|---|
| 13.7 | 🟡 | Stage delete su deals → orphan | Confirmation modal + RESTRICT FK (TASK-07.4) |

---

### 🔴 TASK-14: Stripe billing (Checkout + webhook + idempotency + trial UX)
**Priklausomybės:** TASK-13 · **LOC:** ~700 · **Risk:** HIGH

- **Orchestration:**
  - _MCP_: Stripe (products, prices, webhook events); Supabase (`deploy_edge_function`)
  - _Agents_: `payment-guard`, `security-engineer`, `risk-assessor`
  - _Guards_: `payment-guardian` (PRE+POST), `security-guard` (PRE), `risk-assessor` (PRE)
  - _Skills_: `superpowers:test-driven-development`, `vercel:vercel-functions`
  - _Verify_: Stripe CLI trigger events; Vitest signature reject; Playwright trial→active

- [ ] **14.1** Stripe setup script `scripts/stripe-setup.mjs` (≤ 80 LOC) — creates 1 product + 2 prices (monthly €19, annual €15); writes IDs to `.env.example`
- [ ] **14.2** Migration `<ts>_005_billing_up.sql` — `stripe_events_processed (event_id text PK, processed_at timestamptz)` (≤ 20 LOC)
- [ ] **14.3** `actions/billing.ts:createCheckoutSession(priceId)` (≤ 80 LOC) — Stripe Checkout session su `client_reference_id = org_id`, `subscription_data.metadata.org_id`
- [ ] **14.4** `app/api/stripe/webhook/route.ts` (≤ 250 LOC) — **vienas Vercel Function** atlieka viską: signature verification + idempotency + handlers. Eksplicit `export const runtime = 'nodejs'` (Stripe SDK requires Node), `dynamic = 'force-dynamic'`. [BLOCKS: 14.5]
- [ ] **14.5** `webhooks.constructEvent(payload, sig, secret)` — signature verification PRIVALOMA, 400 if invalid (≤ 15 LOC)
- [ ] **14.6** Idempotency check: `stripe_events_processed.event_id` upsert su `on conflict do nothing returning event_id`; if no row returned → already processed → return 200 (≤ 25 LOC)
- [ ] **14.7** Handler `checkout.session.completed`: UPDATE orgs SET stripe_subscription_id, status='active' (≤ 30 LOC)
- [ ] **14.8** Handler `customer.subscription.updated`: sync `seats_paid`, `current_period_end` (≤ 25 LOC)
- [ ] **14.9** Handler `customer.subscription.deleted`: status='canceled', schedule data retention (≤ 20 LOC)
- [ ] **14.10** Handler `invoice.payment_failed`: email owner via Resend, increment `failed_payments_count` (≤ 30 LOC)
- [ ] **14.11** Handler `invoice.payment_succeeded`: reset `failed_payments_count` (≤ 15 LOC)
- [ ] **14.12** Update `actions/invites.ts:acceptInvite` + `removeMember` — call `stripe.subscriptions.update({ quantity })` su `proration_behavior: 'create_prorations'` (≤ 30 LOC delta)
- [ ] **14.13** `lib/auth/require-active-subscription.ts` (≤ 60 LOC) — checks `orgs.status` + `trial_ends_at`; returns mode `active|trial|read_only|hard_block`
- [ ] **14.14** Trial UX banner `components/shared/TrialBanner.tsx` (≤ 50 LOC) — **2 states**: `trial_active` ("X dienų liko" + Aktyvuoti CTA), `trial_expired` ("Trial baigėsi" + read-only warning + Aktyvuoti CTA)
- [ ] **14.15** Read-only enforcement: Server Actions check `requireActiveSubscription`; UI disable mutation buttons
- [ ] **14.16** Hard block redirect: middleware checks `>30d post-trial` → redirect `/atsiskaitymas`
- [ ] **14.17** Tests: Vitest signature verification (valid + invalid)
- [ ] **14.18** Tests: Vitest idempotency (same event ID 2× → 1 update only)
- [ ] **14.19** Tests: Playwright `tests/e2e/billing.spec.ts` — trial → checkout (test mode) → active → invite member → quantity updates

> **Drop'inta (Simplicity):**
> - Vercel route handler **proxina** į Supabase Edge Function — du sluoksniai webhook'ui = papildomas latency + dvigubas debugging. **Vienas Vercel Function** (14.4) atlieka viską — paprasčiau deploy'inti su app, vienas log stream.
> - TrialBanner 5 states → 2 states. 5-state matrix = premature polish; user'iui užtenka žinoti "trial dar veikia" arba "trial baigėsi".

**Risk per sub-task:**

| Sub | Risk | Issue | Mitigation |
|---|---|---|---|
| 14.5 | 🔴 | Skipped → fake events accepted (FR-21) | Mandatory test 14.17; payment-guard CI gate |
| 14.6 | 🔴 | Idempotency miss → duplicate state (FR-20) | `on conflict do nothing returning` pattern |
| 14.12 | 🟡 | Race: invite accept while subscription updating | Stripe API has built-in retry |
| 14.13 | 🟡 | trial_ends_at TZ bug (FR-23) | Visi UTC; client formatuoja |
| 14.15 | 🟡 | Read-only bypass per direct DB | RLS doesn't enforce sub status; accept risk V1 |

---

### 🟢 TASK-15: E2E test suite + CI/CD pipeline
**Priklausomybės:** TASK-14 · **LOC:** ~500 · **Risk:** LOW

- **Orchestration:**
  - _MCP_: Playwright; Vercel; GitHub Actions
  - _Agents_: `quality-engineer`, `devops-architect`
  - _Guards_: `pre-deploy` (POST), `test-quality-guardian` (POST)
  - _Skills_: `vercel:deployments-cicd`
  - _Verify_: GitHub Actions all jobs pass; Vercel preview URL works; main → prod auto-deploy

- [ ] **15.1** `tests/e2e/_helpers.ts` (≤ 100 LOC) — `signup`, `createWorkspace`, `seedDeal` test helpers
- [ ] **15.2** `tests/e2e/signup-to-deal.spec.ts` (≤ 120 LOC) — Critical scenario #1
- [ ] **15.3** `tests/e2e/invite-accept.spec.ts` (≤ 100 LOC) — Critical scenario #2
- [ ] **15.4** `tests/e2e/cross-tenant-isolation.spec.ts` (≤ 100 LOC) — Critical scenario #3 (Alice ≠ Bob)
- [ ] **15.5** `tests/e2e/billing.spec.ts` (already started in 14.19) — finalize Critical scenario #4
- [ ] **15.6** `.github/workflows/ci.yml` (≤ 150 LOC) — jobs: lint, unit, rls, e2e, guards, deploy:preview (per PR)
- [ ] **15.7** `.github/workflows/deploy.yml` (≤ 80 LOC) — main → Vercel prod deploy
- [ ] **15.8** `vercel.json` su Function regions=fra1 (eu-central-1)
- [ ] **15.9** `README.md` — quickstart su clone → install → setup → run
- [ ] **15.10** `docs/deployment.md` — Stripe webhook URL config, Auth Hook URL config

> **Drop'inta (Simplicity):** Critical scenario #5 (CSV import) — TASK-12 atidėtas į V1.5, scenario grįš kartu.

**Risk per sub-task:**

| Sub | Risk | Issue | Mitigation |
|---|---|---|---|
| 15.4 | 🟢 | Cross-tenant test depends on RLS (already tested) | pgTAP backstop |
| 15.6 | 🟢 | CI flake on Supabase boot | Retry logic + health check |

---

## Execution Strategy

```
PARALLEL GROUP 1 (no deps):
└── TASK-01 Project scaffold

SEQUENTIAL GROUP 2 (depends 01):
└── TASK-02 Supabase setup

SEQUENTIAL GROUP 3 (depends 02) [HIGH RISK, ISOLATED]:
└── TASK-03 Tenancy schema + RLS + Auth Hook 🔴

SEQUENTIAL GROUP 4 (depends 03):
└── TASK-04 pgTAP RLS infrastructure 🟡

PARALLEL GROUP 5 (depends 04):
├── TASK-05 Auth flow 🔴
└── TASK-07 CRM core data layer 🔴

SEQUENTIAL GROUP 6 (depends 05+07):
├── TASK-06 Onboarding 🔴
└── TASK-08 CRM Server Actions 🟡

PARALLEL GROUP 7 (depends 06+08):
├── TASK-09 UI layout 🟢
├── TASK-10 Kanban (no table view) 🟡 (depends 08+09)
└── TASK-11 Contacts + Activities UI (no separate companies/uzduotys) 🟢 (depends 08+09)

SEQUENTIAL GROUP 8 (depends 11+08):
└── TASK-13 Settings 🟡

⏸️ DEFERRED TO V1.5:
└── TASK-12 CSV import — atidėta po V1 launch'o pagal customer feedback

FINAL HIGH-RISK PHASE:
└── TASK-14 Stripe billing 🔴 (depends 13)

VERIFICATION + DEPLOY:
└── TASK-15 E2E + CI/CD 🟢 (depends 14)
```

**Optimaliausia kelionė laikui (po Simplicity peržiūros):**
- Phases 1-2 paraleliai = ~2 d.
- Phase 3 (HIGH RISK) sequential = ~3 d.
- Phase 4 = ~1 d. (drop'intas completeness checker)
- Phase 5 paralleli = ~5 d. (TASK-05 + TASK-07)
- Phase 6 sequential = ~5 d.
- Phase 7 paralleli (3 tracks, slimmer) = ~5 d. (~~7 d.~~ — be table view, be companies UI, be markdown)
- Phase 8 = ~3 d. (~~5 d.~~ — be CSV, be DnD pipeline, be advisory lock)
- Phase 9 (TASK-14, HIGH RISK) = ~4 d. (~~5 d.~~ — be Edge Function proxy, 2-state TrialBanner)
- Phase 10 (TASK-15) = ~2 d. (~~3 d.~~ — be CSV scenario)
- **Total: ~26 dienų ≈ 4 sav. solo dev** (sutaupo ~10 d. vs originalus planas)

---

## Risk Assessment (Phase 3)

### Aggregated risk per parent task

| Task | Risk | Critical paths | Highest sub-risk |
|---|---|---|---|
| 01 Scaffold | 🟢 | none | OKLCH browser support |
| 02 Supabase setup | 🟢 | secrets handling | Service role leak |
| 03 Tenancy + Auth Hook | 🔴 | multi-tenancy | Auth Hook fail = empty CRM |
| 04 pgTAP infra | 🟢 | testing gate | (drop'intas completeness checker; PR review checklist atstoja) |
| 05 Auth flow | 🔴 | auth | Token replay, callback fail |
| 06 Onboarding | 🔴 | tenancy + billing | refreshSession fail (FR-4) |
| 07 CRM data layer | 🔴 | RLS isolation | Policy gap → leak |
| 08 Server Actions | 🟡 | data integrity | Race on stage update |
| 09 UI layout | 🟢 | none | LT diakritika rendering |
| 10 Kanban (no table) | 🟡 | UX critical | Optimistic desync |
| 11 Contacts + Activities UI | 🟢 | none | Combobox on-the-fly company create race |
| ⏸️ 12 CSV import | — | DEFERRED V1.5 | (out of V1 scope) |
| 13 Settings | 🟢 | billing quantity | (advisory lock drop'intas; unique constraint atstoja) |
| 14 Stripe billing | 🔴 | revenue critical | Signature bypass, idempotency |
| 15 E2E + CI/CD | 🟢 | deployment | CI flake |

### Assumptions verified checklist

- [ ] Supabase `custom_access_token_hook` triggers on every token issue → **VERIFY** in TASK-03.5 + integration test 06.10
- [ ] `(select auth.jwt()->...)` SELECT wrap creates initPlan → **VERIFY** via `EXPLAIN ANALYZE` in TASK-07.13
- [ ] Stripe `customer.subscription.updated` fires immediately after quantity change → **VERIFY** via Stripe CLI in TASK-14.20
- [ ] `@dnd-kit/core` keyboard accessibility OOTB → **VERIFY** via Playwright in TASK-10.14
- [ ] Resend SMTP integration with Supabase Auth supports LT chars → **VERIFY** via test email in TASK-05.6
- [ ] Tailwind v4 OKLCH works in Safari < 16.4 → **VERIFY** via Playwright Webkit in TASK-01.13
- [ ] `force row level security` doesn't block `service_role` (webhook handler context) → **VERIFY** + document in TASK-14.4

### Red flags reviewed

✅ **No "replace direct query with hook" tasks** — greenfield project, no legacy
✅ **No "split file" tasks** — files designed within limits from start (file-splitter PRE on each parent)
✅ **No "extract shared constant" — all magic strings traced** — currency, role, type enums in `lib/validation/*.ts`
✅ **No "consolidate duplicate functions"** — Server Actions per-domain
✅ **No "remove type casts"** — generated types regenerate after each migration

### Safe execution order

Order matches Execution Strategy phases (Group 1 → Group 10), prioritizing LOW → HIGH risk within constraints. **Critical: TASK-14 (billing HIGH risk) is LAST before E2E.** Reason: depends on stable foundation; bugs in earlier tasks cascade into billing if not isolated.

---

## Open items

- **OI-1:** Stripe Tax automatinis EU VAT — sprendimas prieš TASK-14.1 (Stripe products setup). Default V1: skip Tax; LT clients first. (PRD §11 Q6)
- **OI-2:** GDPR data residency — Supabase project in Frankfurt. Confirm during TASK-02.1. (PRD §11 Q2)
- **OI-3:** Free tier ar 30-d. trial extension — V2 sprendimas, ne V1 blocker. (PRD §11 Q5)
- **OI-4:** EN UI architektūra — `next-intl` vs `next-i18next` — V2 sprendimas. (PRD §11 Q7)
- **OI-5:** Per-tenant backup script — `pg_dump --schema-only` + custom data export. V2 add-on, ne V1 blocker. (PRD §11 Q3)
- **OI-6:** CSV import (TASK-12) re-evaluation V1.5 — kriterijus: ≥3 customer requests per pirmus 60 d. po launch'o. Iki tol — manual entry per ContactForm.

---

## Simplicity First peržiūros žurnalas (2026-04-26)

Karpathy 2-as principas pritaikytas, ~30% scope'o drop'inta arba supaprastinta:

### Drop'inti elementai (~1500 LOC tausojama)
- **TASK-10.8–9** Deals table view (`sandoriai/sarasas` + `DealsTable.tsx`) — kanban yra V1 diferenciatorius, table = V2.
- **TASK-11.5–6** Atskira `imones/` companies UI — company auto-kuriama per ContactForm Combobox; DB lentelė lieka.
- **TASK-09.6** ThemeToggle dark/light — premature polish.
- **TASK-12 (visas)** CSV import wizard — atidėtas į V1.5 (žr. OI-6).

### Supaprastinti elementai (~700 LOC tausojama)
- **TASK-09.3** OrgSwitcher → statinis org pavadinimo tekstas Sidebar header'yje (V1 = 1 org/user).
- **TASK-13.7** Pipeline reorder DnD → up/down arrow buttons (50 LOC vietoj 200).
- **TASK-13.5** `pg_advisory_xact_lock` invite accept'e → drop'intas (unique constraint atstoja).
- **TASK-14.4** Vercel route handler proxy → Supabase Edge Function → vienas Vercel Function direct.
- **TASK-14.14** TrialBanner 5 states → 2 states.
- **TASK-06.4** `generateUniqueSlug` retry su `-2`, `-3` → `slugifyName` only; unique violation kaip form error.
- **TASK-11.5** Atskira `uzduotys/` page → fold'inta į `veiklos/` su filtrais.

### Smulkūs valymai (~150 LOC tausojama)
- **TASK-01.6** `react-markdown` deps drop → activity body plain text + `pre-wrap`.
- **TASK-01.6** `papaparse` deps drop → CSV atidėtas.
- **TASK-04.3** `check-rls-completeness.sql` meta-checker → PR review checklist'as.

### Bendras tausojimas
- **LOC:** 6000–8000 → 4500–5500 (~30% mažiau).
- **Laikas:** 36 d. → 26 d. (~10 d. tausojama).
- **Dependencies:** drop'inti `react-markdown`, `papaparse`, `@types/papaparse`, `iconv-lite`.
- **Migracijos:** drop'inta `<ts>_004_storage_bucket_up.sql` (CSV bucket).

### Architektūrinė pastaba
"Lean MVP" pavadinimas dabar atspindi turinį: scope'as nukirptas iki tikro V1 minimumo. Po launch'o re-evaluate per customer feedback (OI-6) prieš grąžinant drop'intus elementus.

---

**Susiję dokumentai:**
- PRD: `docs/requirements/REQ-2026-04-26-001-mini-crm.md`
- Spec: `docs/superpowers/specs/2026-04-26-mini-crm-design.md`
- MemPalace: wing `mini_cmr` (drawer'iai + KG triples)
